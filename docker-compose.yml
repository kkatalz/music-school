services:
  # 1. PostgreSQL Database Service
  db:
    image: postgres:14-alpine
    container_name: music_school_db
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER"]
      interval: 5s
      timeout: 5s
      retries: 5
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "${POSTGRES_PORT}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  # 2. NestJS Backend Service
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: music_school_api
    command: npm run start:dev
    volumes:
      - ./backend:/app
      - /app/node_modules # Important to prevent overwriting
    ports:
      - "${API_PORT}:3000"
    environment:
      # Pass the full DATABASE_URL to NestJS
      DATABASE_URL: ${DATABASE_URL}
      PORT: 3000
      JWT_SECRET: ${JWT_SECRET}
    depends_on:
      db:
        condition: service_healthy

  # 3. Streamlit Frontend Service
  web:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: music_school_web
    command: streamlit run main.py --server.port 8501 --server.address 0.0.0.0

    volumes:
      - ./frontend:/app
    ports:
      - "${WEB_PORT}:8501"
    environment:
      # Optionally pass the API host to Streamlit
      API_HOST: ${API_HOST}
    depends_on:
      - api

volumes:
  postgres_data:
